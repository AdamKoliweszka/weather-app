import { DateToTimeOfDayPipe } from './date-to-time-of-day.pipe';

describe('DateToTimeOfDayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToTimeOfDayPipe();
    expect(pipe).toBeTruthy();
  });
});
